import styled from 'styled-components';
import { motion } from 'framer-motion';

export const StyledSkillsList = styled(motion.ul)`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 1.6rem;
  width: 100%;
`;
