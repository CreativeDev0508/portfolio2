import styled from 'styled-components';
import { motion } from 'framer-motion';

export const StyledNavigationContainer = styled(motion.nav)`
  position: fixed;
  top: 0;
  right: 0;
  width: 38rem;
  z-index: 100000;
`;
